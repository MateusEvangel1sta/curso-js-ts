// Dispara um alerta no navegador.
window.alert('Alerta!'); // O uso do Objeto Window não é obrigatório.

// Dispara um "confirm"
let cookies = confirm('Você aceita os cookies?');
console.log(cookies);

// Entrada de dados
let nome = prompt('Qual o seu nome?');
console.log(nome);

// Colocamos os `prompt` e o `confirm` em variáveis para armazenas a resposta do usuário na memória.