const nome = 'Luiz'; // String
const numero = 10; // Number -> int
const numeroDecimal = 10.5; // Number -> null

let indefinido; // undefined -> Não aponta para um local na memória.
let nulo = null; // null -> mesma coisa que undefined.
// Caso queria que uma variável aponta para um lugar inexistente, utilize o null.

const aprovado = true; // Boolean -> true, false (lógico)